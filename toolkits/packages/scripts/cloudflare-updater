#!/bin/bash

echo "[toolkit-scripts] Installing Argo Tunnel agent"

if ! /home/coder/.local/bin/cloudflared-updater; then
  echo "[tooklit-scripts] Failed to install/update Cloudflared. Please bug the image maintainers if symptoms presists."
  exit 1
fi
